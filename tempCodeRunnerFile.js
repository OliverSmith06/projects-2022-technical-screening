if (pos.length >= neg.length){
    //     for (let i = 0; i < numArray.length; i++){
        
    //         if (i % 2 == 0 || i == 0){
    //             output[i] = pos[0];
    //             pos.splice(0, 1);
    //         } else {
    //             output[i] = neg[0];
    //             neg.splice(0, 1);
    //         }
    //     }
    // }
    // if (pos.length < neg.length){
    //     for (let i = 0; i < numArray.length; i++){
        
    //         if (i % 2 == 0){
    //             output[i] = neg[0];
    //             neg.splice(0, 1);
    //         } else {
    //             output[i] = pos[0];
    //             pos.splice(0, 1);
    //         }
    //     }
    // }